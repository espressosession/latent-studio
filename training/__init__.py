"""LoRA automation pipeline (artist -> downloaded -> preprocessed -> captioned
-> trained -> validated -> exported). Importable as a package for local runs
(`python -m training.run_local ...`); also flattened into
notebooks/lora_training_colab.ipynb for Colab (see scripts/build_colab_notebook.py)."""
